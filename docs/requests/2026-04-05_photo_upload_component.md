# Request: PhotoUpload Component

**Date:** 2026-04-05
**User Request:** 请帮我在 components/plan 文件夹下创建一个 PhotoUpload.tsx 组件，包含基本的图片上传 UI。

## Requirements
- Create a new component `PhotoUpload.tsx` in `components/plan/`.
- Must include basic photo upload UI (drag & drop, file selection, preview).
- Design should be modern and premium, consistent with the existing `aquaflow-pro` aesthetics (Framer Motion, Tailwind CSS, Glassmorphism).

## Technical Details
- Framework: Next.js (App Router/React 19).
- Styling: Tailwind CSS 4.
- Icons: Lucide React.
- Animations: Framer Motion.
- Utils: `cn` from `@/lib/utils`.
